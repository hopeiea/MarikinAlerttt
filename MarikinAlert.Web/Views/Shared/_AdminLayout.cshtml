<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Rescue Command</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@@phosphor-icons/web"></script>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;600;900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0b1120;
        }

        .mono {
            font-family: 'JetBrains Mono', monospace;
        }
    </style>
</head>
<body class="text-white h-screen flex flex-col overflow-hidden">

    <header class="bg-[#1e293b] border-b border-slate-700 h-16 flex items-center justify-between px-6 shadow-xl z-50">

        <a asp-controller="Admin" asp-action="Dashboard" class="flex items-center gap-3 hover:opacity-80 transition-opacity">
            <div class="w-8 h-8 bg-red-600 rounded flex items-center justify-center shadow-lg shadow-red-500/20 animate-pulse">
                <i class="ph-fill ph-siren text-white text-lg"></i>
            </div>
            <div>
                <h1 class="text-sm font-black tracking-widest uppercase text-white">Rescue Command</h1>
                <p class="text-[10px] text-slate-400 font-mono tracking-tighter">SECURE CHANNEL • ENCRYPTED</p>
            </div>
        </a>

        <div class="flex items-center gap-6">
            <span class="text-[10px] font-mono text-emerald-500 flex items-center gap-1.5 bg-emerald-500/10 px-2 py-1 rounded">
                <span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span> SYSTEM ONLINE
            </span>

            <div class="h-6 w-px bg-slate-700"></div>

            <div class="flex items-center gap-4">

                <a asp-controller="Admin" asp-action="History" class="text-xs font-bold text-slate-400 hover:text-white uppercase tracking-wider flex items-center gap-1 transition-colors">
                    <i class="ph-bold ph-clock-counter-clockwise"></i> History
                </a>

                <div class="text-right border-l border-slate-700 pl-4">
                    <p class="text-xs font-bold text-white">Officer Admin</p>
                    <p class="text-[10px] text-slate-400">ID: #8821-A</p>
                </div>

                <a asp-controller="Admin" asp-action="Logout" class="bg-slate-700 hover:bg-red-600 text-white p-2 rounded transition-colors" title="Logout">
                    <i class="ph-bold ph-power"></i>
                </a>
            </div>
        </div>
    </header>

    <main class="flex-1 overflow-auto bg-[#0f172a] p-6 relative">
        <div class="absolute inset-0 opacity-[0.03]" style="background-image: linear-gradient(#fff 1px, transparent 1px), linear-gradient(90deg, #fff 1px, transparent 1px); background-size: 40px 40px;"></div>

        <div class="relative z-10 max-w-7xl mx-auto">
            @RenderBody()
        </div>
    </main>

</body>
</html>